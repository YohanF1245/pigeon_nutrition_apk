import 'package:flutter/material.dart';

class MesuresScreen extends StatelessWidget {
  const MesuresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Écran des mesures - En construction'),
    );
  }
} 
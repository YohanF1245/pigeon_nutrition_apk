import 'package:flutter/material.dart';

class EntrainementsScreen extends StatelessWidget {
  const EntrainementsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Écran des entraînements - En construction'),
    );
  }
} 
import 'package:flutter/material.dart';

class RepasScreen extends StatelessWidget {
  const RepasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Écran des repas - En construction'),
    );
  }
} 